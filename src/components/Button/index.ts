import { styled } from "../../styles/stitches.config";

export const ButtonBase = styled('a', {
  fontFamily: "$caption",
  fontSize: "0.9rem",
  fontWeight: 600,
  height: 40,
  width: "max-content",
  padding: "0.6rem 1rem",
  borderRadius: 3,
  cursor: "pointer",
})